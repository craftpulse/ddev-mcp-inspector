#ddev-generated
name: ddev-mcp-inspector

project_files:
  - docker-compose.mcp-inspector.yaml
  - commands/host/mcp-inspector
  - .env.mcp-inspector.example

post_install_actions:
  - |
    # Ensure .ddev directory exists
    mkdir -p .ddev
    if [ ! -f .ddev/.env.mcp-inspector ]; then
      cp .env.mcp-inspector.example .ddev/.env.mcp-inspector
      echo "# Created .ddev/.env.mcp-inspector from example"
    else
      echo "# .ddev/.env.mcp-inspector already exists"
    fi