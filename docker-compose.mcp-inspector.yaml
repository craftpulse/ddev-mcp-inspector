#ddev-generated
# Docker Compose override for MCP Inspector
# Exposes port 3000 from the web container for MCP Inspector access
# This file is managed by ddev-mcp-inspector add-on

services:
  web:
    ports:
      - "6274:6274"
    environment:
      - MCP_INSPECTOR_HOST=0.0.0.0
      - MCP_INSPECTOR_PORT=6274